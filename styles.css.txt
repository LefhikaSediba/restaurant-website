/* Project: FreshBite Restaurant - Redesigned */

/* 1. Root Variables - Upgraded Palette */
:root {
    --primary-color: #1a1a1a;    /* Deep Charcoal */
    --accent-gold: #c49b63;      /* Sophisticated Gold */
    --light-bg: #fcfaf8;         /* Creamy off-white */
    --card-shadow: 0 10px 30px rgba(0,0,0,0.08);
    --transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
}

/* 2. Global Styles */
body {
    font-family: 'Poppins', sans-serif; /* Modern, clean font */
    line-height: 1.7;
    color: #444;
    background-color: var(--light-bg);
}

/* 3. Header & Navigation Styles */
.navbar {
    background-color: #ffffff !important;
    padding: 1.2rem 5%;
    box-shadow: 0 2px 20px rgba(0,0,0,0.04);
}

.navbar-brand {
    font-weight: 800;
    color: var(--primary-color) !important;
    letter-spacing: 2px;
}

.nav-link {
    font-weight: 500;
    color: #555 !important;
    margin: 0 10px;
    transition: var(--transition);
}

.nav-link:hover {
    color: var(--accent-gold) !important;
}

/* 4. Hero/Welcome Section Edits */
/* Note: If you want that full-width look, ensure this container isn't restricted */
.welcome-section {
    padding: 100px 0;
    background: linear-gradient(to right, #ffffff, #f4f7f6);
}

.welcome-section img {
    border-radius: 20px;
    box-shadow: var(--card-shadow);
    transition: var(--transition);
}

.welcome-section img:hover {
    transform: scale(1.02);
}

/* 5. Menu Section - The "Good Look" Upgrade */
.menu-section-title {
    color: var(--primary-color);
    font-size: 2.5rem;
    text-align: center;
    margin-bottom: 3rem;
    position: relative;
}

/* Adding a decorative line under the title */
.menu-section-title::after {
    content: '';
    display: block;
    width: 60px;
    height: 3px;
    background: var(--accent-gold);
    margin: 10px auto;
}

article {
    background: white;
    border-radius: 15px;
    padding: 15px;
    margin-bottom: 30px;
    box-shadow: var(--card-shadow);
    border: 1px solid rgba(0,0,0,0.02);
    transition: var(--transition);
}

article:hover {
    transform: translateY(-12px);
    box-shadow: 0 20px 40px rgba(0,0,0,0.12);
}

/* 6. Buttons & Badges */
.btn-primary, .btn-dark, .order-btn {
    background-color: var(--primary-color) !important;
    border: none !important;
    padding: 12px 25px !important;
    border-radius: 50px !important; /* Pill shape */
    font-weight: 600;
    letter-spacing: 1px;
    transition: var(--transition) !important;
}

.btn-primary:hover, .btn-dark:hover {
    background-color: var(--accent-gold) !important;
    transform: scale(1.05);
}

/* 7. Feedback Form Styles */
.card {
    border: none;
    border-radius: 20px;
    padding: 2rem;
    background: white;
}

.form-control {
    border-radius: 10px;
    padding: 12px;
    border: 1px solid #eee;
    background-color: #f9f9f9;
}

.form-control:focus {
    background-color: #fff;
    border-color: var(--accent-gold);
    box-shadow: 0 0 0 0.25rem rgba(196, 155, 99, 0.15);
}

/* 8. Footer Styles */
footer {
    background-color: var(--primary-color);
    padding: 4rem 0;
    text-align: center;
}
body {
    font-family: 'Poppins', sans-serif;
    background-color: #f8f9fa;
}

/* CSS3 Animation for the Menu Cards */
.menu-card {
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.menu-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 10px 20px rgba(0,0,0,0.15) !important;
}

/* Custom button hover */
.order-btn {
    transition: background-color 0.3s ease;
}

.order-btn:hover {
    background-color: #c49b63 !important; /* Elegant gold hover */
    border-color: #c49b63 !important;
}
/* Force all cards in a row to be the same height */
.row {
    display: flex;
    flex-wrap: wrap;
}

.card {
    height: 100%; /* Makes all cards in a row equal height */
    display: flex;
    flex-direction: column;
}

.card-body {
    display: flex;
    flex-direction: column;
    flex-grow: 1; /* Pushes content to fill the card */
}

.card-body p {
    flex-grow: 1; /* This pushes the button to the absolute bottom */
}

.order-btn {
    align-self: center;
    width: 100%;
}
/* Custom Professional Styles */
body {
    background-color: #f4f7f6; /* Soft light grey background */
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

/* Contact Info Card Styling */
.contact-info-card {
    border-left: 5px solid #212529; /* Dark accent matching your navbar */
    transition: transform 0.3s ease;
}

.contact-info-card:hover {
    transform: translateY(-5px);
}

.icon-box {
    width: 40px;
    height: 40px;
    background: #e9ecef;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    font-size: 1.2rem;
}

/* Form Styling */
.form-control {
    border: 1px solid #dee2e6;
    padding: 0.75rem;
    transition: all 0.3s ease;
}

.form-control:focus {
    box-shadow: 0 0 0 0.25rem rgba(33, 37, 41, 0.1);
    border-color: #212529;
}

.btn-dark {
    letter-spacing: 1px;
    font-weight: 600;
    transition: all 0.3s ease;
}

.btn-dark:hover {
    background-color: #343a40;
    transform: translateY(-2px);
    box-shadow: 0 4px 8px rgba(0,0,0,0.2);
}

footer {
    background: #212529;
    color: white;
    padding: 2rem 0;
    text-align: center;
}