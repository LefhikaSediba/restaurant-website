/* Project: FreshBite Restaurant - Redesigned */

/* 1. Root Variables - Upgraded Palette */
:root {
    --primary-color: #1a1a1a;    /* Deep Charcoal */
    --accent-gold: #c49b63;      /* Sophisticated Gold */
    --light-bg: #fcfaf8;         /* Creamy off-white */
    --card-shadow: 0 10px 30px rgba(0,0,0,0.08);
    --transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
}

/* 2. Global Styles */
body {
    font-family: 'Poppins', sans-serif; /* Modern, clean font */
    line-height: 1.7;
    color: #444;
    background-color: var(--light-bg);
}

/* 3. Header & Navigation Styles */
.navbar {
    background-color: #ffffff !important;
    padding: 1.2rem 5%;
    box-shadow: 0 2px 20px rgba(0,0,0,0.04);
}

.navbar-brand {
    font-weight: 800;
    color: var(--primary-color) !important;
    letter-spacing: 2px;
}

.nav-link {
    font-weight: 500;
    color: #555 !important;
    margin: 0 10px;
    transition: var(--transition);
}

.nav-link:hover {
    color: var(--accent-gold) !important;
}

/* 4. Hero/Welcome Section Edits */
/* Note: If you want that full-width look, ensure this container isn't restricted */
.welcome-section {
    padding: 100px 0;
    background: linear-gradient(to right, #ffffff, #f4f7f6);
}

.welcome-section img {
    border-radius: 20px;
    box-shadow: var(--card-shadow);
    transition: var(--transition);
}

.welcome-section img:hover {
    transform: scale(1.02);
}

/* 5. Menu Section - The "Good Look" Upgrade */
.menu-section-title {
    color: var(--primary-color);
    font-size: 2.5rem;
    text-align: center;
    margin-bottom: 3rem;
    position: relative;
}

/* Adding a decorative line under the title */
.menu-section-title::after {
    content: '';
    display: block;
    width: 60px;
    height: 3px;
    background: var(--accent-gold);
    margin: 10px auto;
}

article {
    background: white;
    border-radius: 15px;
    padding: 15px;
    margin-bottom: 30px;
    box-shadow: var(--card-shadow);
    border: 1px solid rgba(0,0,0,0.02);
    transition: var(--transition);
}

article:hover {
    transform: translateY(-12px);
    box-shadow: 0 20px 40px rgba(0,0,0,0.12);
}

/* 6. Buttons & Badges */
.btn-primary, .btn-dark, .order-btn {
    background-color: var(--primary-color) !important;
    border: none !important;
    padding: 12px 25px !important;
    border-radius: 50px !important; /* Pill shape */
    font-weight: 600;
    letter-spacing: 1px;
    transition: var(--transition) !important;
}

.btn-primary:hover, .btn-dark:hover {
    background-color: var(--accent-gold) !important;
    transform: scale(1.05);
}

/* 7. Feedback Form Styles */
.card {
    border: none;
    border-radius: 20px;
    padding: 2rem;
    background: white;
}

.form-control {
    border-radius: 10px;
    padding: 12px;
    border: 1px solid #eee;
    background-color: #f9f9f9;
}

.form-control:focus {
    background-color: #fff;
    border-color: var(--accent-gold);
    box-shadow: 0 0 0 0.25rem rgba(196, 155, 99, 0.15);
}

/* 8. Footer Styles */
footer {
    background-color: var(--primary-color);
    padding: 4rem 0;
    text-align: center;
}
/* --- CSS3 ANIMATIONS & ADVANCED SELECTORS --- */

/* Modern Grid Layout for the Menu */
.menu-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    padding: 2rem 0;
}

/* Animation: Smooth hover effect using CSS3 Transition */
.menu-card {
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    cursor: pointer;
}

/* Pseudo-class Selector for Interactive Feel */
.menu-card:hover {
    transform: translateY(-15px) scale(1.02);
    box-shadow: 0 20px 40px rgba(0,0,0,0.2);
}

/* Using a CSS3 Variable for the order button */
.order-btn {
    background-color: var(--primary-color);
    transition: background 0.3s ease;
}

.order-btn:active {
    transform: scale(0.95); /* Button 'press' animation */
}